# sub =  find/search and replace
# sub(pattern, replace, string)

import re
pattern = r'name'
text = 'My name is Masud, I name 23 years old.'

# changed_text = re.sub(pattern,'identity', text)
# print(changed_text)

# changed_text = re.sub(pattern,'identity', text,count=1)
# jotobar count dibo, toto jaigai search result k change korbey
# print(changed_text)

# changed_text = re.sub(pattern,'identity', text,count=2)
# jotobar count dibo, toto jaigai search result k change korbey
# print(changed_text)



